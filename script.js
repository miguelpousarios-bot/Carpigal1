let cartCount = 0;let cartTotal = 0;function addToCart(productName, price) {cartCount++;cartTotal += price;document.getElementById('cart-count').textContent = cartCount;document.getElementById('cart-total').textContent = cartTotal + ' €';alert(productName + ' añadido al carrito.');}function finalizarCompra() {if (cartCount === 0) {alert('El carrito está vacío.');} else {window.location.href = 'checkout.html';}}function buscarProducto() {const query = document.getElementById('search').value.toLowerCase();const productos = document.querySelectorAll('.product');productos.forEach(producto => {const nombre = producto.getAttribute('data-name').toLowerCase();producto.style.display = nombre.includes(query) ? 'block' : 'none';});}